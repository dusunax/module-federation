import { vi } from 'vitest';

export const getAllEmotions = vi.fn(async () => []);
export const getEmotionById = vi.fn(async () => null);
export const createEmotion = vi.fn(async () => {});
export const updateEmotion = vi.fn(async () => {});
